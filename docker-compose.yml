version: '3.8'

services:
  hermes:
    image: ${DOCKER_IMAGE:-hermes:latest}
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - STAGING_DELAY_MS=3600000
      - FIREBASE_SERVICE_ACCOUNT=${FIREBASE_SERVICE_ACCOUNT}
    restart: unless-stopped
